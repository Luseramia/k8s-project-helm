apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: us-liquidity-ingress
  namespace: us-liquidity
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/backend-protocol: "HTTP"
    acme.cert-manager.io/http01-ingress-class: "nginx"
    cert-manager.io/cluster-issuer: "letsencrypt-dns01"
spec:
  ingressClassName: nginx
  tls:
    - secretName: us-liquidity-ingress-tls
      hosts:
        - us-liquidity.tarchunk.win
  rules:
    - host: us-liquidity.tarchunk.win
      http:
        paths:
          # host เดียวพอ — /api ให้ nginx ใน frontend pod proxy ไป backend service เอง
          - path: /
            pathType: Prefix
            backend:
              service:
                name: us-liquidity-frontend
                port:
                  number: 80
