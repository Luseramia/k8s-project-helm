apiVersion: v1
kind: Service
metadata:
  name: vault-unseal-bun-service
  namespace: infra
  labels:
    app: vault-unseal-bun
spec:
  selector:
    app: vault-unseal-bun
  ports:
  - name: metrics
    port: 3000
    targetPort: 3000
    protocol: TCP
  type: ClusterIP