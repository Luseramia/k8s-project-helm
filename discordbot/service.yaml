apiVersion: v1
kind: Service
metadata:
  name: discord-bot-service
  namespace: discord-bot
  labels:
    app: discord-bot
spec:
  selector:
    app: discord-bot
  ports:
  - name: metrics
    port: 3000
    targetPort: 3000
    protocol: TCP
  type: ClusterIP