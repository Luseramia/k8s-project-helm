apiVersion: apps/v1
kind: Deployment
metadata:
  name: hpm
  namespace: hpm
  labels:
    app: hpm
spec:
  replicas: 1
  revisionHistoryLimit: 2
  strategy:
    type: RollingUpdate  # เปลี่ยนจาก Recreate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: hpm
  template:
    metadata:
      labels:
        app: hpm
      annotations:
        consul.hashicorp.com/connect-inject: "false"
    spec:
      serviceAccountName: hpm
      dnsPolicy: ClusterFirst
      containers:
        - name: hpm
          image: registry.registry.svc.cluster.local:5000/hpm:10
          imagePullPolicy: Always
          ports:
            - containerPort: 80
              protocol: TCP
            - containerPort: 443
              protocol: TCP
          volumeMounts:
            - name: ssl-certs
              mountPath: /etc/nginx/ssl
              readOnly: true
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
      volumes:
        - name: ssl-certs
          secret:
            secretName: hpm-secrets
            items:
              - key: privatekey
                path: private.key
                mode: 0600
              - key: certificate
                path: certificate.crt
                mode: 0644
      restartPolicy: Always