# Bootstrap manifest kept outside the directory managed by this Application.
# This prevents the Application from treating itself as a child resource.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: parallel-ai-test-orchestrator
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/Luseramia/k8s-project-helm.git
    targetRevision: main
    path: parallel-ai-test-orchestrator
    directory:
      recurse: false
  destination:
    server: https://kubernetes.default.svc
    namespace: ai-test-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
