apiVersion: apps/v1
kind: Deployment
metadata:
  name: x-webapp
  namespace: infra
  labels:
    app: x-webapp
spec:
  replicas: 1
  revisionHistoryLimit: 2
  strategy:
    type: RollingUpdate  # เปลี่ยนจาก Recreate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: x-webapp
  template:
    metadata:
      labels:
        app: x-webapp
      annotations:
        consul.hashicorp.com/connect-inject: "false"
    spec:
      serviceAccountName: x-webapp
      dnsPolicy: ClusterFirst
      containers:
        - name: x-webapp
          image: registry.registry.svc.cluster.local:5000/x-webapp:6
          imagePullPolicy: Always
          ports:
            - containerPort: 80
              protocol: TCP
            - containerPort: 443
              protocol: TCP
          volumeMounts:
            - name: ssl-certs
              mountPath: /etc/nginx/ssl
              readOnly: true
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
      volumes:
        - name: ssl-certs
          secret:
            secretName: x-webapp-secrets
            items:
              - key: privatekey
                path: private.key
                mode: 0600
              - key: certificate
                path: certificate.crt
                mode: 0644
      restartPolicy: Always